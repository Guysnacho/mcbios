import { Schedule } from "@/components/Schedule";

export default function Page() {
  return (
    <div className="min-h-screen bg-linear-to-t from-(--maroon) via-(--pink) to-(--off-white)">
      <Schedule />
    </div>
  );
}
