import { About } from "@/components/About";

export default function Page() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-[var(--maroon)] to-[var(--pink)] via-[var(--off-white)] pt-10">
      <About />
    </div>
  );
}
